// Models/RunningPlanAdaptation.swift
//
// Stub — the actual `RunningPlanAdaptation` @Model class plus the supporting
// `AdaptationReason` / `AdaptationSignal` / `RunningPlanAdaptationResult`
// types live alongside `RunningPlan` in `RunningPlan.swift`.
//
// Why a stub instead of just deleting this file: Xcode's synchronized file
// group cached the path after the file was first created, and a subsequent
// build rejected its disappearance with "Build input file cannot be found".
// Keeping a one-line file here keeps the synced-group cache happy without
// duplicating model code.

import Foundation
